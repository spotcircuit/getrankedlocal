# Copy this file to .env.local and fill in real values. Do NOT commit .env.local
# Database connection for API features (optional for static demo)
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
DATABASE_URL=postgresql://username:password@host/database?sslmode=require
NEXT_PUBLIC_SITE_URL=https://getrankedlocal.com

# Intake form webhook (used by app/intake/page.tsx)
# Example: https://n8n.yourdomain.com/webhook/... or Zapier/Webhook.site URL
NEXT_PUBLIC_INTAKE_WEBHOOK=

# Grid search batch size - number of concurrent browser tabs per batch
# Higher values = faster but more resource intensive
# Railway with 32GB RAM can handle 20-25, smaller servers use 10
GRID_BATCH_SIZE=10
